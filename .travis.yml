---
language: python

python:
    - "3.6"

env:
    - TRAVIS=1

services:
    - mongodb

install:
    - pip install --upgrade pip
    - pip install -r requirements.txt -r requirements-dev.txt .

script:
    - py.test -rxs tests/

notifications:
    email: false

after_success:
    - coveralls
